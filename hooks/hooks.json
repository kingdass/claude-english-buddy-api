{
  "description": "English language coaching — auto-correct prompts and summarize sessions.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "matcher": "*",
        "description": "Intentionally global — the hook script itself filters out slash commands, short prompts, and code patterns before doing any work.",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/prompt-coach-hook.mjs\"",
            "timeout": 60
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/session-end-hook.mjs\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
